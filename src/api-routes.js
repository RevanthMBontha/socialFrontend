const baseURL = "http://localhost:8080/api/v1";

export const USERS_ENDPOINT = `${baseURL}/users`;

export const POSTS_ENDPOINT = `${baseURL}/posts`;

export const LOGIN_ENDPOINT = `${baseURL}/login`;
